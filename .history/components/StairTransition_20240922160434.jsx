import { AnimatePresence, motion } from "framer-motion";
import { usePathname } from "next/navigation";

const StairTransition = () => {
  return (
    <AnimatePresence mode="w">StairTransition</AnimatePresence>
  )
}

export default StairTransition