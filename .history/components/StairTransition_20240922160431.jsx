import { AnimatePresence, motion } from "framer-motion";
import { usePathname } from "next/navigation";

const StairTransition = () => {
  return (
    <AnimatePresence mode>StairTransition</AnimatePresence>
  )
}

export default StairTransition