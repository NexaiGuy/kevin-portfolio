import { AnimatePresence, motion } from "framer-motion";
import { usePathname } from "next/navigation";

const StairTransition = () => {
  return (
    <AnimatePresence mo>StairTransition</AnimatePresence>
  )
}

export default StairTransition